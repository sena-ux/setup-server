# Installasi
```sudo apt install apache2-util```
```htpasswd -c /etc/nginx/.htpasswd codeserver```
#Konfigurasi NGINX
```
location / {
    auth_basic "Restricted";
    auth_basic_user_file /etc/nginx/.htpasswd;

    proxy_pass http://127.0.0.1:8080;
}
```
